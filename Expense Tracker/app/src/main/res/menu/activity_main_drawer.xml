<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_home"
            android:icon="@drawable/ic_home"
            android:title="@string/menu_home"
            android:iconTint="@color/orange"/>
        <item
            android:id="@+id/nav_expense"
            android:icon="@drawable/ic_expanse"
            android:title="@string/expense_details"
            android:iconTint="@color/ic_launcher_background"/>

        <item
            android:id="@+id/nav_graph"
            android:icon="@drawable/ic_baseline_table_chart"
            android:iconTint="@color/gray"
            android:title="Total Expense Table" />


    </group>
    <item android:title="other">
        <menu>

            <item
                android:id="@+id/nav_share"
                android:icon="@drawable/ic_share"
                android:onClick="share"
                android:iconTint="@color/white"
                android:title="Share App"/>

            <item
                android:id="@+id/nav_logout"
                android:icon="@drawable/ic_logout"
                android:onClick="logout"
                android:title="Logout" />

        </menu>

    </item>


</menu>